{
  "name": "late-jump",
  "version": "0.0.0",
  "description": "Show a spinner until the html fragment (hash) is present",
  "main": "index.js",
  "scripts": {
    "test": "npx standard && npx tap --cov test*.js test/*.js",
    "tap": "npx tap --cov test*.js test/*.js",
    "coveralls": "export COVERALLS_REPO_TOKEN=$(cat $HOME/.coveralls-tokens/$npm_package_name); npx tap --cov test*.js test/*.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sandhawke/late-jump.git"
  },
  "keywords": [
    "sandhawke"
  ],
  "author": "Sandro Hawke (http://hawke.org/sandro)",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/sandhawke/late-jump/issues"
  },
  "homepage": "https://github.com/sandhawke/late-jump#readme",
  "devDependencies": {
    "tape": "^4.9.1"
  },
  "dependencies": {
    "debug": "^4.1.0",
    "when-dom-ready": "^1.2.12"
  }
}
